请根据我提供的词汇列表制作 Anki 抽认卡，严格遵守以下规则：

1. **识别语言学原型 (Base Form)**：
   - 必须识别出词族中最基础的词根（通常是动词原形或核心名词/形容词），不要受列表顺序干扰。
   - 示例：输入 "cultural -> culture"，原型应识别为 "culture (n.)"。

2. **制作两类卡片 (使用中文提示)**：
   - **A类 (中文 + 词性 -> 原型)**：
     - 正面：中文释义 + (词性缩写)
     - 背面：对应的英文原型
   - **B类 (原型 + 描述 -> 派生词)**：
     - 正面：[原型] 的 [词性描述]，意为 “[中文释义]”
     - 背面：对应的英文派生词（含反义词、特殊构词）

3. **输出格式要求**：
   - 使用 **制表符 (Tab)** 分隔正面与背面，每张卡片占一行。
   - 按照**词族分组**输出，组与组之间不留空行。
   - 严禁包含编号、标题、Markdown表格或任何额外解释。
   - 输出必须放在一个**代码框**内，方便直接复制。

4. **词性标注规范**：
   - 在 A 类卡片正面必须标注原型词性，如：(n.), (vt.), (adj.)。

待处理词汇列表如下：
[在此粘贴你的词汇列表]
